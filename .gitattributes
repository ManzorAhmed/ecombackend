# Set the default behavior to automatically normalize line endings.
* text=auto

# Explicitly declare files that should always use CRLF line endings on checkout.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Explicitly declare files that should always use LF line endings on checkout.
*.md text eol=lf
